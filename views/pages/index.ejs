<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link type="text/css" rel="stylesheet" href="/public/css/styles.css">
    <title>Admin Event Registration</title>
</head>
<body>
    <h1 class="header">Welcome to the Admin page</h1>
    <div class="form">
        <form action="/events" method="POST">
            <h1 class="header">Event Form</h1>
            <input type="text" name="name" placeholder="New event name" required><br>
            <input type="text" name="description" placeholder="New event description" required><br>
            <input type="date" name="date" required><br>
            <button type="submit">Add Event</button>        
        </form>
    </div>
    <!-- <%- include('../partials/body') %> -->
    <ul>
        <% events.forEach(event => { %>
            <li>
                <div class="event">
                    <div class="event-details">
                        <h3 class="event-name"><%= event.name %></h3>
                        <h5><%= event.date %></h5>
                        <p class="event-desc"><%= event.description %></p>
                    </div>
                    <div class="event-buttons">
                        <button class="edit-button"><a href="/events/<%= event.id %>/edit">Edit</a></button>
                        <form action="/events/<%= event.id %>/delete" method="POST" style="display:inline;">
                            <button class="delete-button" type="submit">Delete</button>
                        </form>
                    </div>
                </div>
            </li>
        <% }); %>
    </ul>
    <%- include('../partials/footer') %>
</body>
</html>